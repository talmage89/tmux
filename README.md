# tmux config

## Setup

Clone TPM (Tmux Plugin Manager) into the `plugins` directory:

```sh
git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins
```

Then install plugins by launching tmux and pressing `prefix` + `I` (or run `~/.config/tmux/plugins/tpm/bin/install_plugins` from the command line).
